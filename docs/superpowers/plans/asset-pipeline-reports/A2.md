# A2 report — crypto + integrity

**Agent:** A2 · **Date:** 2026-08-09 · **Status:** code + fixtures done;
test run BLOCKED by a foreign compile break (A4's `AssetStore.kt`) — see
Verification.

## Files created

- `app/src/main/java/org/dhamma/gong/assets/OpenSslSaltedAes.kt`
  - `KeyIv`, `evpBytesToKeyMd5(password, salt)` — EVP_BytesToKey, MD5, one
    round per block: key = D1‖D2 (32 B), iv = D3 (16 B).
  - `decryptingStream(input, passphrase)` — consumes the 16-byte
    `Salted__`+salt header (`IllegalArgumentException` on bad/truncated
    magic), then wraps the rest in a `CipherInputStream`
    (`AES/CBC/PKCS5Padding`). Streaming only; nothing buffered whole.
    Passphrase encoded UTF-8 via `CharBuffer` (no `String` minted);
    intermediate password/key/iv byte arrays zeroed after cipher init
    (`SecretKeySpec`/`IvParameterSpec` clone their inputs).
- `app/src/main/java/org/dhamma/gong/assets/Integrity.kt`
  - `sha256Hex(input)` — streaming 64 KiB chunks, closes input, lowercase hex.
  - `sniff(header)` — first bytes → `Magic`: `ID3`, MPEG frame sync
    (0xFF + 0xE0 mask), `Salted__`, `<` or UTF-8 BOM + `<` → HTML, empty →
    EMPTY, else OTHER. Truncated headers degrade to OTHER, never crash.
- `app/src/test/java/org/dhamma/gong/assets/OpenSslSaltedAesTest.kt` (7 tests)
- `app/src/test/java/org/dhamma/gong/assets/IntegrityTest.kt` (12 tests)
- `app/src/test/resources/crypto/plain.bin`, `.../crypto/salted.bin`

No `Log.*` anywhere; no key/iv/passphrase in any message or test output.
`AssetTypes.kt` untouched.

## Fixture provenance (dev machine, system OpenSSL 3.6.3, 2026-08-09)

Throwaway TEST passphrase: `niyam-fixture-2026`. No production passphrase
appears anywhere.

- `plain.bin`: 3072 deterministic bytes (SHA-256 chain seeded from
  `niyam-crypto-fixture`), starts with ASCII `ID3` so it sniffs as MP3.
  SHA-256 `b529bf1e8552cdd73e4342e9284f40cf356df8b4e468ccdd045a37c7fab90fee`.
- `salted.bin` (3104 B, random salt, `Salted__` header verified):

  ```
  openssl enc -aes-256-cbc -md md5 -pass pass:niyam-fixture-2026 \
      -in plain.bin -out salted.bin
  ```

- KDF vector embedded in `OpenSslSaltedAesTest` from:

  ```
  openssl enc -aes-256-cbc -md md5 -pass pass:niyam-fixture-2026 \
      -S 00112233445566FF -P
  key=B580E7E8AE44B892ABD41857D6B3EB299E9FFF3CB0CA357D689CD16193D732CF
  iv =C47B18C7D1F522DB15916DF3A7E2F1D1
  ```

## Test coverage

- KDF matches the OpenSSL `-P` vector (key + iv).
- Decrypt fixture → byte-equal to `plain.bin`, SHA-256 matches, sniffs ID3.
- Bad magic and truncated header → `IllegalArgumentException`.
- Wrong passphrase → `IOException` with `BadPaddingException` cause on
  drain — loud failure, not silent garbage.
- `sha256Hex`: FIPS vectors for `""` and `"abc"`, 200 KiB multi-chunk
  stream vs direct `MessageDigest`, input actually closed.
- `sniff`: all six `Magic` values incl. 0xFF 0xFB and 0xFF 0xE0 sync,
  failed sync mask, BOM+`<`, truncated 1–3 byte headers → OTHER, empty →
  EMPTY.

## Verification

- `:app:testDebugUnitTest --tests "org.dhamma.gong.assets.OpenSslSaltedAesTest"
  --tests "org.dhamma.gong.assets.IntegrityTest"` could NOT run: `kspDebugKotlin`
  fails in **A4's `assets/AssetStore.kt`** ("Missing '}'" at 154:6, "Unclosed
  comment" at 266:1). Waited 60 s and retried per plan rules, plus one extra
  attempt after the file's mtime advanced — same two errors all three times.
  The file appears abandoned mid-write. Not mine to fix; Lead should nudge A4
  or repair, then re-run the command above.
- The compiler reports errors **only** in `AssetStore.kt` across the whole
  module, so A2's four Kotlin files at least parse cleanly; type-check and
  test execution are pending the foreign fix.
- Independent fixture verification done outside gradle:
  `openssl enc -d -aes-256-cbc -md md5 -pass pass:niyam-fixture-2026 -in
  salted.bin | shasum -a 256` reproduces plain.bin's hash
  (`b529bf1e…b90fee`), and the KDF vector embedded in the test came straight
  from `openssl -P`, so the fixtures are internally consistent.

## Deviations

- Added two extra guard tests beyond the brief (truncated envelope header,
  empty stream) — same API, no behaviour change.
