# A3 report — catalog loader + CDN downloader

**Status:** DONE — 16/16 tests green
(`./gradlew :app:testDebugUnitTest --tests "org.dhamma.gong.assets.AssetCatalogTest" --tests "org.dhamma.gong.assets.CdnDownloaderTest"`).
First attempt hit a transient compile error in A4's `AssetStore.kt` mid-write;
after the mandated 60 s wait the retry compiled and passed.

## Files created

- `app/src/main/java/org/dhamma/gong/assets/AssetCatalog.kt` — kotlinx.serialization
  (`ignoreUnknownKeys = true`), maps manifest rows to pinned `AudioAsset`
  (`id` = filename minus `.mp3`), `byId` accepts id or full filename,
  missing `encrypted_sha256` defaults to `""` (parse tolerates, fetch refuses).
- `app/src/main/java/org/dhamma/gong/assets/CdnDownloader.kt` — HttpURLConnection
  only, 64 KiB buffer, inline SHA-256, resume re-hashes the prefix then sends
  `Range: bytes=<size>-`; 200-to-Range restarts from zero; progress throttled
  to 256 KiB; plain-sentence failure reasons; retryable: timeout/5xx/truncation
  true, 404/bad-URL/empty-hash false; deletes dest on hash/size/HTML, keeps
  partial on truncation. Comment notes HttpURLConnection refuses cross-protocol
  redirects.
- `app/src/test/java/org/dhamma/gong/assets/AssetCatalogTest.kt` — 6 tests.
- `app/src/test/java/org/dhamma/gong/assets/CdnDownloaderTest.kt` — 10 tests,
  raw `ServerSocket` fake server mirroring `ShellyClientTest`.
- `app/src/test/resources/catalog/doha_manifest.json` — byte copy of the
  bundled manifest.

## Test results

- `AssetCatalogTest`: 6 tests, 0 failures — real-manifest parse (11 assets,
  cdn_base), D06 hashes/sizes spot check, byId with id and with full filename
  plus unknown-id null, D01 vs D09 distinctness, unknown fields ignored +
  missing checksum defaults to "", garbage JSON throws.
- `CdnDownloaderTest`: 10 tests, 0 failures — happy path (exact bytes,
  monotonic throttled progress, no Range header), resume/206 with prefix
  re-hash, 200-to-Range restart, HTML page with 200 (dest deleted), `<` first
  byte with lying Content-Type (dest deleted), truncation (retryable, partial
  kept), checksum mismatch (dest deleted, exact reason string), 404
  non-retryable, 500 retryable, empty expected hash refused with zero requests.

## Deviations / notes for the Lead

- Added 3 tests beyond the required list (500 retryable, `<`-body with
  non-HTML Content-Type, empty-hash refusal) — they pin behaviour the brief's
  SEMANTICS section requires but the test list omitted.
- `byId` equality treats a lookup by full filename as the same asset as by id
  (map hit on `id`, fallback strips `.mp3`).
- Retryable choices not pinned by the brief: hash/size mismatch and HTML page
  = retryable true (transient CDN/captive-portal plausible); unexpected 206
  without Range = retryable true; invalid URL / empty catalog hash /
  final-size-under-1024 = retryable false.
- HTML rejection deletes dest in both detection paths (Content-Type header and
  first body byte) per the brief; truncation keeps the partial.
- The shared `app/build` directory is being used by parallel agents — a later
  agent's filtered test run replaces the JUnit XML results of an earlier one.
  Counts above were re-captured immediately after a clean re-run.
