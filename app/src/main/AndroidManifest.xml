<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The scheduler and player run with the screen off. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- The persistent notification is the appliance's health indicator. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Power-loss recovery: re-arm the schedule after a reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--
      The app *is* an alarm clock: a missed 04:00 gong is the failure we exist
      to prevent (design doc §09). USE_EXACT_ALARM is non-revocable and needs a
      Play policy justification; SCHEDULE_EXACT_ALARM is the revocable fallback
      requested at first run on devices that deny it.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Only requested when staff pick a Bluetooth output route. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!--
      The amplifier relay (Shelly 1 Gen4) on the centre's own WiFi. This is the
      app's only network use: no cloud, no analytics, no Shelly Cloud. See
      res/xml/network_security_config.xml.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Doha downloads check connectivity and metering before spending data. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
      Network reads the SSID for the informational Network screen. Both are
      normal permissions with no runtime prompt. ACCESS_FINE_LOCATION is
      deliberately NOT requested: it is what Android really wants before it will
      name a WiFi network on API 29+, and a location grant to caption an
      informational screen is a bad trade. The screen says the name is withheld.
    -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GongAppliance">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.GongAppliance">
            <!--
              Fixed landscape: the appliance UI targets ~1280×800 (10" tablet
              landscape). sensorLandscape allowed reverse-landscape flips that
              broke screenshots and kiosk mounts.
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.GongService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <!--
              No LOCKED_BOOT_COMPLETED: gong.db lives in credential-encrypted
              storage, so a direct-boot start would crash before first unlock
              (FABLE-REVIEW B7).
            -->
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!--
          The third keep-alive belt's landing point. Not exported: the only
          thing that may trigger it is our own kickstart alarm, armed by
          LivenessWorker or BootReceiver. Alarm delivery grants the short
          allowlist window in which a mediaPlayback FGS may be started.
        -->
        <receiver
            android:name=".service.KickstartReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="org.dhamma.gong.KICKSTART" />
            </intent-filter>
        </receiver>

        <!-- Any of these invalidates the materialized day; the scheduler re-resolves. -->
        <receiver
            android:name=".service.TimeChangeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.DATE_CHANGED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
