*.iml
.gradle/
/local.properties
# The legacy CDN media passphrase. Never commit it — see media.properties.example
# for the shape and AGENTS.md "Media key" for why it lives in its own file.
/media.properties
/.idea/
.DS_Store
/build/
/app/build/
**/build/
/captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.ap_
*.dex
*.class
bin/
gen/
out/
.kotlin/
*.jks
*.keystore
secrets/
.DS_Store
*.swp
*~

# Agent skill mirrors are committed intentionally (Claude/Grok discovery).
# Do not re-install with: npx skills add ... -a '*'  (creates dozens of vendor dirs)
